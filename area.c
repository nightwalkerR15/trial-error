int area(int a);
main()
{
int len;
printf("enter the lenght of rectangle");
scanf("%d",&len);
int area(int a);

return 0;
}
int area(int a )
{
    int len;
 a= len*len;
 printf("%f",a);
 return a;
}
